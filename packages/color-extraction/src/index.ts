export type { ColorPair, ExtractionOptions } from './types';
export {
  extractColors,
  extractColorsFromUrl,
  extractColorsFromBuffer,
} from './extractor';
export { getDefaultColors, normalizeImageInput } from './utils';
