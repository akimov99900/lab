{
  "version": 2,
  "rootDirectory": "packages/bearbrick-app",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm run build:dependencies && cd packages/bearbrick-app && pnpm run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}